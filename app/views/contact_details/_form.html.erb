<%= form_with(model: contact_detail, local: true) do |form| %>
  <% if contact_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_detail.errors.count, "error") %> prohibited this contact_detail from being saved:</h2>

      <ul>
        <% contact_detail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contact_id %>
    <%= form.number_field :contact_id %>
  </div>

  <div class="field">
    <%= form.label :contact_detail_info %>
    <%= form.text_field :contact_detail_info %>
  </div>

  <div class="field">
    <%= form.label :contact_detail_type %>
    <%= form.text_field :contact_detail_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
