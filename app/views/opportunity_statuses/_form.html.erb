<%= form_with(model: opportunity_status, local: true) do |form| %>
  <% if opportunity_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opportunity_status.errors.count, "error") %> prohibited this opportunity_status from being saved:</h2>

      <ul>
        <% opportunity_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :opportunity_status_id %>
    <%= form.number_field :opportunity_status_id %>
  </div>

  <div class="field">
    <%= form.label :oppotunity_status_name %>
    <%= form.text_field :oppotunity_status_name %>
  </div>

  <div class="field">
    <%= form.label :process %>
    <%= form.text_field :process %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
